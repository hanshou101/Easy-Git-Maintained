# .frac.md — docs

> 生成规则：本文件只总结当前目录的直属文件，以及直属子目录的 `.frac.md`。
> 最近更新：2026-08-10 10:35 +0800
> 输入范围：direct-files + child-fracs only

## 1. 本目录是什么

Easy Git 的深入技术说明和品牌媒体目录。用户入门留在根 README；这里解释一次同步的保证、分类规则和 API 提交流程，并存放被 README/发布页面引用的视觉资源。

## 2. 直属文件

- `how-it-works.md`：按固定远端快照、规范 push 表示扫描、三态分类、冲突、pull 表示落盘、原子 commit/ref 更新到状态持久化的顺序解释一次 mapping 同步；同时定义 blob SHA 身份、工作区/仓库换行边界、排除、Device Flow、状态栏和明确非目标。
- `logo.svg`：256×256、带可访问标题的矢量 Logo，组合紫色圆角底、文件夹和 Git branch 图形。
- `logo.png`：512×512 RGBA 位图版 Logo，供不支持 SVG 的展示面使用。

## 3. 子目录导航

- `screenshots/`：见 `screenshots/.frac.md`；功能截图、演示视频和成套宣传图。

## 4. 本层约束 / 约定 / 禁忌

- `how-it-works.md` 描述的是可依赖行为；同步顺序、方向语义、换行表示、冲突或 API 写法改变时必须同步更新。
- SVG 与 PNG 是同一品牌资产的两种载体，改图形或颜色时保持二者一致。
- 截图展示真实 UI；界面术语或流程变化时下钻检查相关媒体是否过期。

## 5. 什么时候需要下钻

- 理解同步保证、冲突矩阵或原子提交机制时读 `how-it-works.md`。
- 改 README/发布视觉时读 `screenshots/.frac.md` 并核对 Logo 文件。
