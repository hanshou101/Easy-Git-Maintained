# .frac.md — docs/screenshots

> 生成规则：本文件只总结当前目录的直属文件，以及直属子目录的 `.frac.md`。
> 最近更新：2026-08-10 10:16 +0800
> 输入范围：direct-files + child-fracs only

## 1. 本目录是什么

README、文档和发布宣传使用的界面媒体资源。这里保存真实插件操作界面，不含业务代码。

## 2. 直属文件

- `demo.gif`、`demo.mp4`：960×624 GIF 演示及对应 MP4 视频。
- `mapping-modal.png`、`repo-picker.png`、`settings.png`：mapping 编辑、远端仓库选择和完整设置界面的功能截图。
- `marketing-1-hero.png` 至 `marketing-5-wikilinks.png`：一组 1200×800 宣传图，依次覆盖产品概览、文件夹选择、同步方向、认证和 wikilink 能力。

## 3. 子目录导航

无直属子目录。

## 4. 本层约束 / 约定 / 禁忌

- 文件名被上层文档引用，替换资源时保持引用同步，避免留下断图。
- 功能截图应与当前 UI 和术语一致；不要仅压缩或转码其中一个版本而让 GIF/MP4 演示内容分叉。
- 二进制媒体不作为源码编辑，修改前确认尺寸、格式和仓库体积影响。

## 5. 什么时候需要下钻

- 更新 README 的截图、演示或宣传素材时，再打开对应媒体核对画面内容。
