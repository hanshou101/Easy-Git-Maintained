# .frac.md — .

> 生成规则：本文件只总结当前目录的直属文件，以及直属子目录的 `.frac.md`。
> 最近更新：2026-08-10 10:35 +0800
> 输入范围：direct-files + child-fracs only

## 1. 本目录是什么

Easy Git 1.7.2 的独立长期维护版 Obsidian 插件仓库：把一个或多个 vault 文件夹映射到 GitHub 或自托管 Forgejo/Gitea 的仓库路径，支持 push、pull、双向同步、冲突处理、工作区/仓库换行策略、Markdown 渲染转换和本地备份。`src/` 是源码真相，根部 `main.js`、`manifest.json`、`styles.css` 是可安装构建产物。

## 2. 直属文件

- `.gitignore`：排除依赖、source map、日志、环境变量和编辑器目录；不要把这些生成/本地文件提交。
- `.npmrc`：设置版本 tag 不自动添加 `v` 前缀。
- `package.json`：Node/npm 入口；`build` 先严格 TypeScript 检查再 production esbuild，`lint` 运行 Obsidian 规则，两个 `test:*` 脚本运行回归验证，`version` 更新版本表。
- `package-lock.json`：锁定 esbuild、TypeScript、Obsidian、差异合并和 lint 依赖，安装应使用 `npm ci`。
- `esbuild.config.mjs`：以 `src/main.ts` 为入口，外置 Obsidian/Electron，输出 ES2022 CommonJS `main.js`；production 才压缩且不留 source map。
- `eslint.config.mjs`：TypeScript 项目 lint 与 Obsidian 推荐规则；明确豁免生成 bundle、mjs 和依赖目录，并保留项目对 proper-noun 文案与受控日志的例外。
- `tsconfig.json`：ES2022、Bundler resolution、strict/noImplicitAny/noFallthrough 等编译约束，覆盖 `src/**/*` 且不发射文件。
- `manifest.json`：Obsidian 插件 id/name/version、最低 Obsidian 1.6.6、作者及桌面兼容性声明。
- `versions.json`：历史插件版本到最低 Obsidian 版本的兼容表；新增版本必须由 `version-bump.mjs` 维护。
- `version-bump.mjs`：读取 npm 目标版本，同步 manifest 版本和 versions 映射并 stage 两个文件。
- `main.js`：由 esbuild 从 `src/main.ts` 生成的已提交 bundle；不要手工编辑，构建后与 manifest/styles 一起核对。
- `styles.css`：设置页、mapping 行、状态栏、modal、冲突和诊断报告的插件样式；类名与 `src/ui`/`src/settings.ts` 配套。
- `README.md`：独立维护版定位，以及安装、认证、mapping/destination、方向、换行策略、转换、冲突、备份、隐藏路径、日志和构建的用户契约；示例资源来自 `docs/`。
- `CONTRIBUTING.md`：贡献者安装、严格 TS/UI 禁令、目录架构、provider 扩展边界和 PR 验证要求。
- `MAINTENANCE.md`：规定 `hanshou101/Easy-Git-Maintained` 为开发/发布权威、原作者仓库只作 upstream 参考，并定义 HugeRepo 在子仓合并后更新 gitlink 与安装产物的顺序。
- `SECURITY.md`：私密漏洞报告渠道、token/路径/网络安全范围与 release attestation 验证方式。
- `LICENSE`：MIT 许可文本。

## 3. 子目录导航

- `.github/`：见 `.github/.frac.md`；Issue/PR 协作模板和 tag 发布流水线。
- `docs/`：见 `docs/.frac.md`；同步机制说明、Logo 和截图媒体。
- `scripts/`：见 `scripts/.frac.md`；CRLF 与隐藏路径回归脚本。
- `src/`：见 `src/.frac.md`；插件编排、共享类型、provider、同步核心和 UI。

## 4. 本层约束 / 约定 / 禁忌

- 依赖安装用 `npm ci`；交付前至少运行 `npm run lint`、`npm run build` 及两个回归脚本，生成 bundle 必须与源码同一提交。
- `main.js`、`manifest.json`、`styles.css` 是发布面；源码改动后不能只提交其中一部分，也不能直接修 bundle 绕过 TypeScript。
- token 优先存系统 keychain，旧版 fallback 才进入插件数据；同步只写 mapping 范围并排除安全目录，禁止引入 telemetry 或第三方网络。
- provider 变更必须保持 GitHub 路径字节级兼容；push 采用非 force、单父提交，冲突和 pull 覆盖必须遵守备份契约。
- 每个 mapping 的 push/pull 换行策略默认 `preserve`；LF/CRLF 转换只处理可能的文本、保留 BOM、二进制直通，扫描 SHA 必须等于实际上传 blob。
- 源码变更在本独立仓完成并重建 `main.js`；**合并不是终点**：合并后必须完成 HugeRepo 集成（先检出 `Awesome_ObsidianPlugin_HugeRepo` 并读其 `SUBMODULE_INTEGRATION.md`，本仓在其中就是 `submodules/Easy-Git`），由单独 HugeRepo PR 更新 gitlink 和 `dist/Easy-Git`，禁止发布临时分支产物。

## 5. 什么时候需要下钻

- 改同步行为先读 `src/.frac.md` 及其 `sync/`、`github/` 子摘要；改交互先读 `ui/`。
- 改发布或版本时读 `.github/.frac.md`、`package.json`、`manifest.json` 和 `version-bump.mjs`。
- 改用户承诺、媒体或安全边界时分别读 `README.md`、`docs/.frac.md`、`SECURITY.md`。
